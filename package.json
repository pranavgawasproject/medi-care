{
  "name": "medicare",
  "version": "2.0.0",
  "private": true,
  "description": "MediCare clinic OS — Next.js 15 App Router + Supabase",
  "license": "MIT",
  "scripts": {
    "dev": "npm --prefix frontend-next run dev",
    "build": "npm --prefix frontend-next run build",
    "start": "npm --prefix frontend-next run start",
    "lint": "npm --prefix frontend-next run lint",
    "test": "npm --prefix frontend-next run test"
  },
  "engines": {
    "node": ">=22"
  }
}
